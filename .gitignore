# Dependencies
node_modules/
# package-lock.json은 커밋한다 (CI 재현성 + setup-node npm 캐시 요구). 무시 금지.

# Android build outputs
android/app/build/
android/.gradle/
android/build/
android/local.properties
android/capacitor-cordova-android-plugins/

# Keystore (security)
android/release.keystore

# iOS
ios/App/Pods/
ios/App/App.xcworkspace/

# Env
.env
.env.local

# OS
.DS_Store
Thumbs.db

# Supabase CLI temp
supabase/.temp/

# QA 검증 산출물 (스크린샷 — 재생성 가능, 무게 큼. 검증 스크립트는 추적)
screenshots/verify/
