# ARC — Adult Running Club · 프로젝트 현황표

> **G2 Company Ltd** | 럭셔리 러닝 멤버스클럽 앱
> 작성일: 2026-06-09 | 브랜치: `claude/project-status-dashboard-5qz2wb`

---

## 1. 한눈에 보기

| 항목 | 값 |
|------|-----|
| 제품 | ARC — Running Society (서울 프리미엄 러닝 멤버십) |
| 앱 ID | `com.g2company.arc` |
| 버전 | 1.0.0 |
| 라이브 데모 | https://kgg2512.github.io/AdultRunningClub/ |
| 기술 스택 | HTML5/CSS/JS (PWA) · Supabase · Cloudflare Workers · Capacitor 6 |
| 배포 | GitHub Pages (web) · Google Play (Android AAB) |
| 현재 동작 모드 | **DEMO_MODE (Supabase 자격증명 placeholder)** ⚠️ |

---

## 2. 개발 단계 (Phase)

| Phase | 내용 | 상태 |
|-------|------|------|
| Phase 1 | 프로토타입 — 멤버십 가입 흐름 | ✅ 완료 |
| Phase 2 | 결제 연동 (Stripe) | ⏳ 예정 |
| Phase 3 | 앱 런칭 (Play Store) | ⏳ 예정 |

---

## 3. 영역별 완성도

| 영역 | 상태 | 비고 |
|------|------|------|
| 프론트엔드 (PWA) | ✅ | `index.html` 단일 파일, Gate→인증→피드→마이페이지→월드맵 |
| PWA 설정 | ✅ | `manifest.webmanifest`, `sw.js`, `offline.html` |
| 아이콘/스플래시 | ✅ | adaptive 아이콘, 모든 mipmap 밀도 완비 |
| Supabase 스키마 | ✅ | 5개 테이블 + RLS 정책 + Storage 정책 (`supabase/schema.sql`) |
| Supabase 라이브 연결 | ⚠️ | `index.html`에 `YOUR_SUPABASE_URL` placeholder → DEMO_MODE 강제 |
| Android (Capacitor) | ✅ | 프로젝트 완비, AAB 빌드 파이프라인 구성 |
| iOS | 🟡 | 템플릿만 존재 (`ios-templates/`), cap 프로젝트 미추가 |
| Cloudflare Worker | ✅ | `cf-worker/email-proxy.js` (이메일 OTP 프록시) |
| 법무 (개인정보/약관) | ✅ | `privacy.html`, `terms.html` — CLO 감사 반영, 국외이전 동의 포함 |
| 스토어 리스팅 | ✅ | `store-assets/STORE_LISTING.md`, 스크린샷 5종, 피처 그래픽 |
| 결제 (Stripe) | ⛔ | 미착수 (Phase 2) |
| CI/CD | 🔴 | **Android 빌드 실패 중** (아래 참고) |

---

## 4. 🔴 즉시 조치 필요 (블로커)

| # | 이슈 | 영향 | 원인 / 해결 |
|---|------|------|------------|
| 1 | **CI Android 빌드 연속 실패** | AAB 산출물 생성 불가 → Play 배포 차단 | `setup-node@v4`의 `cache: 'npm'` + `npm ci`가 **`package-lock.json` 부재**로 실패. lockfile 커밋하거나 cache 설정 제거 필요 |
| 2 | **Supabase 자격증명 placeholder** | 프로덕션에서도 DEMO_MODE로만 동작 (실인증/DB 미사용) | `index.html`의 `SUPABASE_URL`/`SUPABASE_ANON_KEY`를 실제 값으로 주입 (빌드시 치환 권장) |
| 3 | **`index.html` ↔ `www/index.html` 비동기** | 79.2KB vs 77.5KB — 웹과 앱 번들 내용 불일치 | `npm run sync` 실행 후 커밋 (현재 CI 실패로 자동 동기화 안 됨) |

---

## 5. 최근 작업 흐름 (git log)

최근 커밋은 **법무(개인정보·약관) 보강**과 **DEMO_MODE 안정화**에 집중:

- `design:` DEMO_MODE 사용자 안내 문구 개선 + CSS 인라인 제거
- `legal:` 국외이전·법령보존 조항 추가 (개인정보보호법 §28-8, 전자상거래법 §6)
- `fix:` 개인정보 국외이전 동의 체크박스 추가
- `fix(arc):` DEMO_MODE 회원가입 플로우 (Supabase 없이 데모 인증)
- `security:` 하드코딩된 anon key 제거 → 복원 (public key 판단)

---

## 6. 다음 권장 액션 (우선순위)

1. **[P0] CI 복구** — `package-lock.json` 생성·커밋 또는 워크플로우 cache 옵션 정리
2. **[P0] Supabase 실연결** — 자격증명 빌드 주입 + DEMO_MODE 해제 검증
3. **[P1] www 동기화** — `npm run sync` 후 커밋, 웹/앱 일치
4. **[P1] AAB 서명 빌드** — keystore secrets 4종 확인 후 release 번들 산출
5. **[P2] Stripe 결제 (Phase 2)** 착수
6. **[P2] iOS 프로젝트 추가** — `npx cap add ios`

---

*Generated by Claude Code · 2026-06-09*
